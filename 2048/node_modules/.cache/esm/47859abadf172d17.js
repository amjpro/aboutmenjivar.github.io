let keypress,Game;_df1‍.w("keypress",[["default",["keypress"],function(v){keypress=v}]]);_df1‍.w("./engine/game",[["default",["Game"],function(v){Game=v}]]);


keypress(process.stdin);


/**
 * The code in this file is used to run your game in the console. Use it
 * to help develop your game engine.
 *
 */
let game = new Game(4);
//  game.loadGame({
//      board:[32,16,32,16,8,2,16,8,32,8,0,32,8,16,64,16],
//      score: 0,
//      won:false,
//      over:false
//  })
_df1‍.g.console.log(game.toString());

game.onMove(gameState => {
    _df1‍.g.console.log(game.toString());
    // console.log(game.gameState);
});

game.onWin(gameState => {
    _df1‍.g.console.log('You won with a gameState of...', gameState)
});

game.onLose(gameState => {
    _df1‍.g.console.log('You lost! :(', gameState)
    console.log(`Your score was ${gameState.score}`);
});

process.stdin.on('keypress', function (ch, key) {
    switch (key.name) {
        case 'right':
            game.move('right');
            break;
        case 'left':
            game.move('left');

            break;
        case 'down':
            game.move('down');

            break;
        case 'up':
            game.move('up');
            break;
    }
    if (key && key.ctrl && key.name == 'c') {
        process.stdin.pause();
    }
});


process.stdin.setRawMode(true);
process.stdin.resume();

